<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.sichu.system.mapper.MenuMapper">
    <select id="selectPermissionByUserId" resultType="java.lang.String">
        SELECT DISTINCT t1.permission
        FROM t_sys_menu AS t1
        LEFT JOIN t_sys_role_menu AS t2 ON t2.menu_id = t1.id
        LEFT JOIN t_sys_role AS t3 ON t3.id = t2.role_id
        LEFT JOIN t_sys_user_role AS t4 ON t4.role_id = t3.id
        LEFT JOIN t_sys_user AS t5 ON t5.id = t4.user_id
        WHERE t5.id = #{userId}
        AND t1.status = 1
        AND t1.permission IS NOT NULL
    </select>

    <select id="selectListByRoleCode"
            resultType="cn.sichu.system.model.entity.MenuDO">
        SELECT t1.*
        FROM t_sys_menu AS t1
        LEFT JOIN t_sys_role_menu AS t2 ON t2.menu_id = t1.id
        LEFT JOIN t_sys_role AS t3 ON t3.id = t2.role_id
        WHERE t3.code = #{roleCode}
        AND t1.status = 1
    </select>
</mapper>